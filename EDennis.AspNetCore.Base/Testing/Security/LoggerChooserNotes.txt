NEED TO SIMPLIFY LoggerChooser:
	Just BackupLogger or 

	GetInputData(ScopeProperties)
	  returns KEY|VALUE as KEY to BE USED TO SELECT LOGGER INDEX


LoggerChooser : Dictionary<string,int>
	SetLoggerIndex(ScopeProperties){
		ScopeProperties.LoggerIndex = this.[GetInputData() ?? "default"]
	}



- 